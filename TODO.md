# TODO

## 存储中心术语调整（公开数据集/公开模型）

- [x] 将“版本”文案统一改为“提供者”（示例：openmoss、openai、qwen）。
- [x] 保持后端字段兼容：数据库和 API 仍沿用 `version` 字段名，避免迁移风险。
- [x] 前端错误映射增加“资源提供者不合法”。

## 规划：上传自建数据集、自训练模型

### 目标

- [ ] 支持用户将自建数据集、自训练模型纳入“公开数据集/公开模型”目录。
- [ ] 支持可追踪、可校验、可恢复（失败可重试）的上传流程。
- [ ] 支持大文件和断点续传，避免浏览器单次上传失败导致全量重传。

### 方案对比

- 方案 A：从容器中选择目录导入（服务端拉取）
	- 优点：不走公网上传，速度快；适合已在平台内训练完成的产物。
	- 风险：目录权限和越权读取风险高；需要严格白名单和审计。
	- 适用：管理员、内部节点、同机房高速网络。
- 方案 B：客户端打包上传（浏览器/CLI 上传）
	- 优点：权限边界清晰；体验直观；最通用。
	- 风险：超大文件易中断；前端需要分片、续传、进度管理。
	- 适用：普通用户、自建模型产物从本地或外部环境导入。
	- 增强：支持 Personal Access Token（PAT）驱动的 CLI/SDK 上传，提升目录上传、断点续传和自动化体验。
- 方案 C：从网盘导入（平台服务端拉取）
	- 优点：用户只需要把数据放到网盘或先转存分享链接；真正的大流量下载发生在存储节点，避开浏览器长连接、家庭宽带上行和本地断电等不稳定因素。
	- 风险：阿里云盘、百度网盘的接口、会员限速、分享链接策略和风控规则会变化；需要把第三方工具视为“可替换下载适配器”，不能和核心资源入库逻辑强绑定。
	- 适用：用户已有阿里云盘/百度网盘资源、跨地域网络不稳定、数据集/模型体量较大（几十 GB 到 TB 级）。

### 客户端上传 + Personal Access Token 方案细化

#### 定位

- [ ] 将方案 B 拆成两个入口：
	- 浏览器上传：沿用登录态/session，面向小文件、少量文件、临时补充上传。
	- CLI/SDK 上传：使用个人 Access Token，面向目录、大文件、自动化脚本和可恢复上传。
- [ ] PAT 不是为了让浏览器长期持有 token，而是给用户本地 CLI、实验室脚本、CI 任务使用。
- [ ] 大体量资源的首选仍是方案 C（网盘导入）或方案 A（服务端目录导入）；PAT 解决的是“客户端上传更方便、更可恢复”，不能消除本地网络上行差、机器休眠、跨境链路不稳等天然问题。

#### PAT 设计

- [ ] 用户在个人设置中创建 PAT：
	- 名称：例如 `lab-upload-cli`。
	- 权限范围：最小化 scope，例如 `resource:upload`、`resource:read-own`、`task:read-own`。
	- 过期时间：7 天、30 天、90 天、自定义；默认建议 30 天。
	- 可随时撤销；撤销后立即拒绝新请求，正在进行的上传会话进入可恢复但需重新认证状态。
- [ ] Token 格式：
	- 明文只展示一次，例如 `svps_pat_` 前缀，便于用户识别和平台日志脱敏。
	- 后端只保存 hash、前缀、后 4 位、scope、过期时间、最后使用时间、创建 IP/UA 摘要。
	- 审计日志记录 token 名称和 token id，不记录明文。
- [ ] 安全策略：
	- PAT 仅允许 HTTPS API；禁止通过 query string 传 token，只接受 `Authorization: Bearer <token>`。
	- 支持管理员禁用 PAT、限制最长有效期、限制普通用户可创建的 token 数。
	- 可选：绑定 IP/CIDR、单 token 并发上限、每日上传流量上限。

#### CLI/SDK 上传流程

- [ ] 提供官方 CLI，例如：

```bash
server-vps-upload \
  --endpoint https://platform.example.com \
  --token svps_pat_xxx \
  --type dataset \
  --name my-dataset \
  --provider self \
  --tags custom,vision \
  --path ./dataset
```

- [ ] CLI 调用上传 API：
	- `POST /api/resource-upload/sessions`：创建上传会话，提交资源元信息、文件 manifest、总大小、文件数。
	- `GET /api/resource-upload/sessions/{id}`：查询会话、已上传分片、失败原因。
	- `PUT /api/resource-upload/sessions/{id}/parts/{part_id}`：分片上传。
	- `POST /api/resource-upload/sessions/{id}/complete`：提交合并和校验。
	- `POST /api/resource-upload/sessions/{id}/cancel`：取消会话。
	- `GET /api/tasks/{task_id}`：查询发布/校验任务状态。
- [ ] CLI 负责：
	- 扫描目录生成 manifest：相对路径、大小、mtime、sha256（可配置是否全量计算）。
	- 分片大小默认 64MB，可根据文件大小和网络自动调整到 32-128MB。
	- 并发上传、失败重试、断点续传、本地上传状态缓存。
	- 支持 `--dry-run`、`--resume`、`--include/--exclude`、`--bandwidth-limit`、`--concurrency`。

#### 服务端处理

- [ ] 上传目标先进入用户暂存区：`{base}/.uploads/{user_id}/{session_id}/`。
- [ ] 合并完成后进入校验任务：
	- 校验 manifest、分片 hash、最终文件 hash（如启用）。
	- 路径安全检查：禁止绝对路径、`..`、空路径、超深目录、超长文件名。
	- 文件数量、单文件大小、总大小按用户角色/配额限制。
- [ ] 发布策略：
	- 普通用户上传后进入待审核资源或个人暂存资源。
	- 管理员可直接发布到 `{dataset_base_path|model_base_path}/{name}/{provider}`。
	- 所有发布动作复用共享资源注册、扫描、入库和审计逻辑。

#### 使用边界

- [ ] 建议产品提示：
	- 几十 MB 到几 GB：浏览器上传。
	- 几 GB 到几十 GB：CLI + PAT + 分片续传。
	- 几十 GB 到 TB 级：优先网盘导入或服务端目录导入。
- [ ] 失败提示需要明确区分：
	- token 过期/撤销/权限不足。
	- 用户配额不足或目标资源名冲突。
	- 分片缺失、hash 不匹配、上传会话过期。
	- 服务端暂存空间不足或发布校验失败。

### 网盘导入方案细化

#### 推荐架构

- [ ] 新增“网盘导入”入口，作为方案 C：
	- 用户填写资源类型、名称、提供者、标签、网盘类型、网盘路径或分享链接、提取码（可选）。
	- 如果是分享链接，优先要求用户先转存到自己的网盘目录，再由平台从“用户网盘路径”拉取；直接解析分享链接仅作为后续增强。
	- 平台创建导入任务：`pending -> preparing -> downloading -> verifying -> publishing -> ready/failed`。
	- 下载任务在 storage/mixed 节点执行，先写入暂存目录：`{base}/.imports/{task_id}/`，成功校验后再原子移动到公开资源目录。
	- 任务完成后复用现有“共享资源注册 + 校验 + 入库”流水线，不为网盘导入另建一套资源模型。
- [ ] 下载适配器分两层：
	- 统一层：平台只关心 `list`、`download`、`stat`、`resume`、`cancel`、`progress`、`cleanup`。
	- Provider 层：阿里云盘、百度网盘、后续 123Pan/OneDrive/S3 等各自实现，允许替换工具和参数。

#### 实现选型

- [ ] 选型 1：AList 作为统一网盘连接器（推荐 V1）
	- 部署内部 AList，仅暴露给管理后端/下载任务容器，不直接暴露公网。
	- AList 已提供阿里云盘、百度网盘等存储驱动，并提供 WebDAV 与文件 API；平台可通过 WebDAV/rclone 或 AList API 把远端目录复制到 storage 节点。
	- 优点：多网盘统一接入，后续新增网盘成本低；平台不用直接维护每个网盘的登录和目录 API。
	- 注意：阿里云盘旧的 Share 驱动不稳定，优先使用 Aliyundrive Open；百度网盘大文件下载常需要特定 User-Agent，且 SVIP/限速会影响体验。
- [ ] 选型 2：Provider CLI 下载容器（推荐作为兜底）
	- 阿里云盘可评估 `tickstep/aliyunpan`：支持 Linux、多用户、目录下载、断点续传、单文件并行下载、同步备份。
	- 百度网盘可评估 `BaiduPCS-Go`：支持目录下载、断点续传、单文件并行下载、分享链接转存。
	- 优点：直接利用社区 CLI 的下载能力，调参与日志更直观。
	- 风险：第三方 CLI 会受官方接口变化影响；需要版本锁定、健康检查、失败提示和替换机制。
- [ ] 选型 3：HTTP/Aria2 离线下载（只作为普通直链补充）
	- 对可公开访问的 HTTP/HTTPS 直链可直接用 aria2/SimpleHttp 下载。
	- 不作为阿里云盘/百度网盘主路径，因为网盘链接往往需要登录态、UA、临时签名或会员能力。

#### 任务与可靠性设计

- [ ] 下载前预检：
	- 检查网盘凭据是否可用，列出目录，估算文件数和总大小。
	- 检查 storage 节点剩余空间，要求至少 `资源大小 * 1.1` 的可用空间。
	- 生成 manifest：路径、大小、mtime、可用 hash（如果 provider 能提供）。
- [ ] 下载过程：
	- 使用 `.partial` 暂存目录，禁止未完成文件进入公开资源目录。
	- 支持任务取消、失败重试、限速、并发数配置；同一用户/同一网盘账号限制并发，避免触发风控。
	- 断点续传优先交给下载适配器实现；平台层记录已完成文件和总进度。
	- 任务日志保留最近 N KB，前端展示关键失败原因（登录失效、限速、空间不足、文件不存在、风控/验证码）。
- [ ] 校验与发布：
	- 下载后再次扫描 manifest，校验文件数量、大小和可用 hash。
	- 支持用户提供 `SHA256SUMS` 或 `manifest.json`，有则强校验，无则至少做大小与文件数校验。
	- 发布时从暂存目录原子移动到 `{dataset_base_path|model_base_path}/{name}/{provider}`。
	- 入库后记录来源：`source_type=cloud_drive`、`cloud_provider`、`source_path/share_url_hash`、`import_task_id`。

#### 安全与权限

- [ ] 凭据管理：
	- 不保存明文 cookie/password；仅保存 refresh token / access token / AList storage id 等最小凭据，并加密存储。
	- 支持用户手动解绑网盘账号；解绑后新任务不可创建，历史任务只保留审计摘要。
	- 管理员可配置是否允许普通用户绑定个人网盘；敏感部署可仅允许管理员创建网盘导入任务。
- [ ] 路径与内容安全：
	- 网盘路径只能导入到任务暂存目录，禁止用户指定服务端任意路径。
	- 解压能力默认关闭；如后续支持压缩包导入，必须做 Zip Slip 防护、文件数/深度/总大小限制。
	- 发布公开资源需要管理员审核或管理员权限；普通用户默认只能导入到个人暂存区/待审核资源。
- [ ] 审计：
	- 记录操作者、网盘类型、资源元信息、远端路径 hash、文件数、总大小、开始/结束时间、校验结果、失败原因。

#### 产品流程

- [ ] 上传向导的来源增加：`本地上传`、`容器目录导入`、`网盘导入`、`Hugging Face/ModelScope`。
- [ ] 网盘导入页：
	- 第一步：选择网盘类型并绑定/选择凭据。
	- 第二步：选择“网盘路径”或“分享链接 + 提取码”；分享链接提示建议先转存。
	- 第三步：预检文件清单、总大小、目标路径和发布信息。
	- 第四步：创建任务，跳转任务中心查看进度。
- [ ] 任务中心展示：
	- 下载速度、已下载/总大小、文件数、当前文件、重试次数、限速/风控提示。
	- 失败任务支持“重试”“重新授权”“复制诊断信息”。

#### 外部资料与约束记录

- AList 文档：支持阿里云盘、百度网盘等驱动，并提供 WebDAV；WebDAV 支持表中包含 AliyunDrive 和 BaiduNetdisk。
- AList 阿里云盘文档：旧的 Aliyundrive / Share 驱动标记为不推荐，提示可能不稳定并建议使用 Aliyundrive Open。
- AList 百度网盘文档：大于约 20MB 的文件下载通常需要携带 `User-Agent: pan.baidu.com`，官方接口稳定但大文件速度和会员状态相关。
- AList API：提供 `/api/fs/list`、`/api/fs/get`、`/api/fs/add_offline_download` 等接口，可作为统一接入层参考。
- `tickstep/aliyunpan`：阿里云盘 CLI，支持多平台、目录下载、断点续传、单文件并行下载和同步备份。
- `BaiduPCS-Go`：百度网盘 CLI，支持目录下载、断点续传、单文件并行下载和分享链接转存。

### 推荐路线（混合）

- [ ] V1 优先上线方案 C（网盘导入）和已有 Hugging Face/ModelScope 下载入口，解决大体量资源进入平台的问题。
- [ ] V1 同时保留方案 B（客户端上传）作为小文件、小数据集和兜底入口；大文件上传不作为首推路径。
- [ ] V1.1 增加方案 A（容器目录导入）作为管理员高级能力。
- [ ] 所有入口最终汇聚到同一“资源注册 + 校验 + 入库”流水线。

### 分阶段实施

- [ ] 第 1 阶段：数据模型与接口
	- 公开资源元信息统一：`resource_type`、`name`、`version(语义=provider)`、`tags`、`source_url`、`size`、`checksum`。
	- 新增 PAT 数据模型与接口：创建、列表、撤销、更新过期时间、记录最后使用时间。
	- 新增上传会话接口：创建会话、分片上传、合并提交、取消会话。
	- 新增网盘导入任务接口：创建任务、预检、开始/取消、重试、查看日志、重新授权。
	- 新增目录导入接口（管理员）：选择容器 + 路径 + provider + 标签。
- [ ] 第 2 阶段：传输与可靠性
	- 分片上传（建议 32-128MB 分片），支持断点续传与秒传（按分片 hash）。
	- 提供 CLI/SDK 上传工具，支持 PAT、manifest、本地上传状态缓存、并发、限速、断点续传和重试。
	- 网盘导入使用服务端下载任务，支持限速、并发、断点续传、失败重试和 `.partial` 暂存目录。
	- 并行上传与限流；失败分片重试；会话过期自动清理。
	- 后端异步任务化（队列 + 状态机）：`pending -> transferring -> verifying -> ready/failed`。
- [ ] 第 3 阶段：校验与安全
	- 文件结构校验（模型目录完整性、数据集关键清单）。
	- 校验和（SHA256）与大小复核；路径越界防护；压缩包解包防 Zip Slip。
	- PAT 只保存 hash，支持 scope、过期、撤销、审计和管理员禁用策略。
	- 权限模型：普通用户仅可上传到自己的暂存区；管理员可发布到公开区。
	- 审计日志：记录谁、何时、从哪里上传/导入了什么。
- [ ] 第 4 阶段：产品体验
	- 上传向导：资源类型 -> 名称 -> 提供者 -> 标签 -> 来源（上传/容器目录）-> 提交。
	- 任务中心展示实时进度、失败原因与一键重试。
	- 发布前预览：文件数、总大小、校验摘要。

### 验收标准

- [ ] 50GB 文件上传中断后可续传，重试成功率 > 99%。
- [ ] 导入/上传完成后 100% 进入可浏览、可挂载状态。
- [ ] 未授权用户无法读取或导入其他用户容器目录。
- [ ] 全链路可审计（上传者、来源、时间、校验结果）。
